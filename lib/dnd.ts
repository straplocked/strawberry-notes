/**
 * Custom MIME type for internal drag-and-drop of a note id.
 * Using a scoped type keeps external text/file drags out of our drop targets.
 */
export const DRAG_MIME = 'application/x-sn-note-id';
